# Syntax Stress Tests

These scripts are deliberately dense, harmless examples for testing linters,
pretty-printers, syntax highlighters, parsers, and runtime parity. They use
`test/more` and should emit TAP with no `not ok` lines.
